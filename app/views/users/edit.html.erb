<%= form_for @user, url: user_path(@user), html: { method: :patch } do |f| %>
  <h1>ユーザー 編集</h1>
  <p>氏名 <%= f.text_field :name %></p>
  <p>年齢 <%= f.text_field :age %></p>
  <input type="submit" value="送信" >
<% end %>
<%= link_to "一覧画面へ", user_path %>
